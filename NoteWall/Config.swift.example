import Foundation

/// Configuration file for sensitive credentials
/// 
/// INSTRUCTIONS:
/// 1. Copy this file to Config.swift (it's gitignored)
/// 2. Replace the placeholder PIN with your actual 8-digit PIN
/// 3. Never commit Config.swift to git
/// 
/// This is similar to .env.local in Next.js - keeps secrets out of version control
struct Config {
    /// 8-digit developer PIN for promo code admin access
    /// Replace with your actual secure 8-digit PIN
    static let developerPIN = "00000000" // ⚠️ REPLACE THIS WITH YOUR ACTUAL PIN
    
    // MARK: - Analytics
    /// Mixpanel project token
    /// Get this from https://mixpanel.com → Project Settings → Access Keys
    static let mixpanelToken = "YOUR_MIXPANEL_PROJECT_TOKEN" // ⚠️ REPLACE THIS
}

